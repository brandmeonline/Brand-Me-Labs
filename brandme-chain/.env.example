# Copyright (c) Brand.Me, Inc. All rights reserved.
#
# Brand.Me Chain Service - Environment Configuration
# ===================================================

# Server
PORT=3001
ENVIRONMENT=development

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Logging
LOG_LEVEL=info

# ==========================================
# Cardano Configuration
# ==========================================

# Network (mainnet, testnet, preprod, preview)
CARDANO_NETWORK=preprod

# Wallet Configuration (choose ONE method)
# Method 1: Mnemonic phrase (24 words) - RECOMMENDED
CARDANO_MNEMONIC_PATH=/secrets/cardano-mnemonic.txt

# Method 2: Private key (Bech32 format)
# CARDANO_WALLET_PATH=/secrets/cardano-wallet.key

# Blockfrost API Key (required for real transactions)
# Get your API key at: https://blockfrost.io
BLOCKFROST_API_KEY=your_blockfrost_project_id

# Fallback Mode (true = use simulated transactions if SDK fails)
CARDANO_FALLBACK_MODE=true

# ==========================================
# Midnight Configuration
# ==========================================

# Network (mainnet, testnet, devnet)
MIDNIGHT_NETWORK=testnet

# Wallet Path (loaded from K8s secrets in production)
# DO NOT commit actual wallet files to git!
MIDNIGHT_WALLET_PATH=/secrets/midnight-wallet.key

# RPC URL (optional - defaults based on network)
MIDNIGHT_RPC_URL=https://midnight-testnet.example.com

# Fallback Mode (true = use simulated transactions - Midnight SDK in development)
MIDNIGHT_FALLBACK_MODE=true

# ==========================================
# Development Notes
# ==========================================

# To get started with Cardano:
# 1. Get a Blockfrost API key from https://blockfrost.io (free tier available)
# 2. Generate a wallet using cardano-cli or use existing mnemonic
# 3. Fund the wallet with testnet ADA from https://docs.cardano.org/cardano-testnet/tools/faucet/
#
# For mainnet:
# - Use CARDANO_NETWORK=mainnet
# - Ensure wallet has sufficient ADA for transaction fees
# - BLOCKFROST_API_KEY must be for mainnet project
#
# For Midnight:
# - SDK is currently in development by IOG
# - MIDNIGHT_FALLBACK_MODE should remain true until SDK is released
# - Architecture is future-ready for when SDK becomes available

# ==========================================
# Production Deployment (Kubernetes)
# ==========================================

# In production, secrets are mounted from Kubernetes:
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: blockchain-secrets
# type: Opaque
# data:
#   cardano-mnemonic: <base64-encoded-mnemonic>
#   midnight-wallet: <base64-encoded-wallet-key>
#   blockfrost-api-key: <base64-encoded-api-key>
#
# Then mount as files:
#   volumeMounts:
#     - name: blockchain-secrets
#       mountPath: /secrets
#       readOnly: true
