# Copy to .claude/.env.local and fill in real values.
# .claude/.env.local is gitignored. Never commit real secrets.
#
# Read at session start by .claude/hooks/session-start.sh and exported
# into the session environment so MCP servers in .mcp.json can pick them up.

# 21st.dev Magic MCP server -- get a key at https://21st.dev/magic/console
MAGIC_API_KEY=
